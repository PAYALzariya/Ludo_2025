using UnityEngine;
using UnityEngine.UI;
using TMPro;

public class GiftNotificationPopup : MonoBehaviour
{
    public TMP_Text recivername, senderName,quntityText;
    public Image profileimage, giftimage;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
